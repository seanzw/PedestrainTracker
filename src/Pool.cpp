#include "Pool.h"
/*
template<typename T> T &Pool<T>::operator[](uint index) const {
	return data[index];
}

template<typename T> void Pool<T>::Push(const T &t) {
	if (size == data.size())
		data.push_back(t);
	else 
		data[size] = t;
	size++;
}

template<typename T> void Pool<T>::clear() {
	size = 0;
}
*/